FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 5173

# 컨테이너 기동 시 npm install 재실행 — package-lock이 최신이면 즉시 skip되므로 빠름.
# 이 단계가 있어야 package.json 변경 후 이미지 재빌드 없이도 새 패키지가 설치됨.
CMD ["sh", "-c", "npm install && npm run dev"]
